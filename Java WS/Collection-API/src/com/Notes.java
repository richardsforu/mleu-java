package com;

public class Notes {
	
	/*
	 * 	NOTES
	 * --------
	 * 	-> In java java.lang.Object is the super  class to all the classes in java
	 *  -> COllection object can store any type of object
	 *  -> COllection can perfom adding, retrieving, updating and deleting kind of operations
	 *  
	 *  
								Collection is the top level interface
								List extends Collection
								Set extends Collection
								
								
								Collection
						List				 	Set
								
									
									
	 *  
	 *  
	 *   Main Parts of Collection api
	 *   ------------------------------
	 *   1. Interfaces
	 *   2. Implementation classes
	 *   3. Methods in the implementation classes. (Operations)
	 *   
	 *   
	 *   INterfaces
	 *   ------------
	 *   	1. List
	 *   	2. Set
	 *   	3. Map
	 *   
	 *   1. List interface
	 *   ----------------
	 *   	-> Its an indexed based interface
	 *   	-> Duplicates accepted
	 *   	-> Insertion order is same
	 *   
	 *   Implementation classes for this List interface
	 *   ------------------------------------------------
	 *   1. ArrayList
	 *   2. Vector
	 *   3. LinkedList
	 *   4. Stack
	 *   
	 *   2. Set Interface
	 *   ------------------
	 *   	-> Set interface is non indexed 
	 *   	-> No Duplicates allowed
	 *   	-> Insertion order is not guarenteed. 
	 *   
	 *   Implementation classes for this Set interface
	 *   ------------------------------------------------
	 *   1. HashSet
	 *   2. LinkedHashSet
	 *   3. TreeSet
	 *   
	 *   
	 *   3. Map Interface
	 *   -----------------
	 *   -> Its non indexed interface
	 *   -> carries key and value pares
	 *   -> Key must be unique and value can be non unique
	 *   -> Insertion order is not guareented
	 *   
	 *    Implementation classes for this Map interface
	 *   ------------------------------------------------
	 *   1. HashMap
	 *   2. Hashtable
	 *   3. LinkedHashmap
	 *   4. TreeMap
	 *   
	 *   
	 * 
	 */

}
